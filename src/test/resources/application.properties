spring.application.name=langchain4j-demo
spring.threads.virtual.enabled=true

# Logging configuration
# Logging is configured in logback-test.xml
# You can override log levels in tests: @SpringBootTest(properties = {"logging.level.com.example.demo=DEBUG"})

# Test-specific ports to avoid conflicts
ollama.port=11435
elasticsearch.port=9201

